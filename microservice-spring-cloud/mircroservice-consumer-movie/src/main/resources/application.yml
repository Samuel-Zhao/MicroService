server:
  port: 7901
spring:
  application:
    name: MICROSERVICE-CONSUMER-MOVIE
user:
  userServicePath: https://localhost:7900/simple
eureka:
 client:
   healthcheck:
    enabled: true
   serviceUrl:
    defaultZone: http://localhost:9876/eureka/
 instance:
  prefer-ip-address: true